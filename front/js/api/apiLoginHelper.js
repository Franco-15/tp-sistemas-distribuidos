const port = 3000


export function postCredentials(username, password) {
/*
    const credentials = [username, password];

    //todo ver si la ruta esta bien
    return axios.POST(`http://localhost:${port}/api/login`, credentials) 
    .then(response => {

        
    })
    .catch(error => {
        console.error("Error en la solicitud:", error);
        return [];
    });
*/
}